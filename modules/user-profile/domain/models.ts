export type Account = {
  username: string;
  email: string;
};

export type AccountDetails = {
  username: string;
};

export type PasswordChange = {
  currentPassword: string;
  newPassword: string;
};
